# KamaChat 项目开发与面试指南

> 本文用于项目复盘、简历准备和面试表达。回答基于当前代码现状，不能把“建议优化”描述成“已经完成”。

## 一、项目背景

### 1. 项目定位

KamaChat 是一个仿微信的即时通讯系统，目标是完整串联账号体系、联系人关系、单聊群聊、实时通信、消息持久化、缓存、文件传输、音视频信令和后台管理等典型 IM 场景。

普通 CRUD 项目主要围绕 HTTP 请求展开，而聊天系统还需要解决：

- 如何维护大量长连接和在线用户状态。
- 如何实时转发单聊和群聊消息。
- 如何让离线用户重新上线后看到历史消息。
- 如何处理消息持久化、缓存和状态更新。
- 如何在本地开发模式与 Kafka 分布式模式之间切换。
- 如何维护好友、申请、拉黑、群成员等复杂状态。

### 2. 项目价值

该项目适合用于展示以下能力：

- Go 并发模型和 Channel 的实际应用。
- WebSocket 双向通信与在线连接管理。
- MySQL 业务建模和 GORM 数据访问。
- Redis 缓存、验证码和缓存失效设计。
- Kafka 在 IM 消息链路中的扩展思路。
- Vue 3 前端工程、Pinia 状态管理和 WebRTC。
- Windows、Docker 与跨平台配置改造能力。

### 3. 当前版本边界

当前版本适合学习、演示和二次开发，不应直接描述为生产级 IM：

- 基础文字聊天、好友、群聊、文件消息和后台管理已实现。
- Kafka 和 WebRTC 有代码实现，但生产可靠性仍需完善。
- 表情包、语音消息、聊天记录按钮和全文复制尚未完成。
- 已完成 bcrypt、HTTP JWT、管理员中间件和 WebSocket JWT 身份校验；Token 吊销、对象级授权、消息可靠投递、分布式在线状态和对象存储仍需升级。

## 二、开发准备

### 1. 环境

- Windows 10/11
- Docker Desktop
- Go 1.20+
- Node.js、npm
- VS Code
- Git

### 2. 本地服务职责

| 服务 | 运行位置 | 地址/端口 | 作用 |
|---|---|---|---|
| Vue 前端 | Windows 主机 | 127.0.0.1:8080 | 页面和浏览器客户端 |
| Go 后端 | Windows 主机 | 127.0.0.1:8000 | HTTP、WebSocket、业务逻辑 |
| MySQL | Docker | 主机 13306 → 容器 3306 | 持久化用户、关系、会话、消息 |
| Redis | Docker | 主机 16379 → 容器 6379 | 验证码和热点数据缓存 |
| Kafka | 可选 | 9092 | 分布式消息转发模式 |

### 3. 启动步骤

```powershell
# 根目录：启动 MySQL 和 Redis
docker compose up -d

# 根目录：启动 Go 后端
go mod download
go run .\cmd\kama_chat_server

# web/chat-server：启动前端
npm.cmd install
npm.cmd run serve
```

访问 `http://127.0.0.1:8080`。

本地验证码默认为 `123456`。

### 4. 开发前检查

```powershell
docker compose ps
Test-NetConnection 127.0.0.1 -Port 13306
Test-NetConnection 127.0.0.1 -Port 16379
Test-NetConnection 127.0.0.1 -Port 8000
```

如果执行 `npm` 被 PowerShell ExecutionPolicy 拦截，改用 `npm.cmd`。

### 5. 配置原则

- 后端默认读取 `configs/config.toml`。
- 其他环境通过 `KAMA_CHAT_CONFIG` 指向独立配置，不要直接反复修改默认文件。
- 前端通过 `VUE_APP_BACKEND_URL`、`VUE_APP_WS_URL` 切换环境。
- 本地默认 `messageMode=channel`，不要求 Kafka。
- 只有要验证 Kafka 链路时才启动 Kafka 并切换配置。
- 生产环境必须通过 `KAMA_CHAT_JWT_SECRET` 提供至少 32 字符的高熵随机密钥，不能使用仓库中的本地开发密钥。

## 三、后端开发总结

### 1. 分层结构

后端采用相对清晰的分层：

- Controller：`api/v1`，绑定请求、调用服务、统一返回 JSON。
- DTO：`internal/dto`，区分请求和响应结构。
- Service：`internal/service/gorm`，承载用户、好友、群、会话和消息业务。
- Model：`internal/model`，定义 GORM 表结构。
- Infrastructure：MySQL、Redis、Kafka、短信、日志和静态资源。
- Realtime：`internal/service/chat`，维护 WebSocket 客户端及消息转发。

### 2. 数据库设计

系统使用用户、群、联系人、申请、会话和消息六类核心实体。

设计亮点：

- 联系关系和申请记录分离，便于表达“申请中、通过、拒绝、拉黑”等流程。
- 单聊和群聊共用消息表，通过接收者 UUID 前缀区分类型。
- 会话表保存最近消息和时间，便于快速生成会话列表。
- GORM 软删除保留历史数据，管理员接口可使用 Unscoped 查询。

现有不足：

- 数据库缺少更严格的联合唯一索引和外键约束。
- 群成员 JSON 与联系人表形成冗余，需要事务保证一致。
- 历史明文密码通过首次正确登录渐进迁移为 bcrypt；新账号只保存 bcrypt 哈希。

### 3. WebSocket 与 Channel

每个在线用户对应一个 Client：

- `Conn`：WebSocket 连接。
- `SendTo`：客户端级待发送缓冲。
- `SendBack`：服务端写回浏览器的缓冲。

聊天服务器维护：

- 在线客户端 `map[userID]*Client`。
- Login Channel。
- Logout Channel。
- Transmit Channel。
- Mutex，保护在线 Map。

消息流：浏览器 → Client.Read → Transmit → Server → MySQL → 接收者 SendBack → Client.Write → 浏览器。

Channel 满时，消息先进入 Client.SendTo；两层缓冲都满才返回繁忙提示。这展示了基本背压思路，但尚未实现严格的优先级、公平性和持久化重试。

### 4. Channel 与 Kafka 双模式

配置 `messageMode=channel` 时，消息在当前 Go 进程内转发，适合本地开发，部署简单、延迟低。

配置 `messageMode=kafka` 时，Client 将消息写入 Kafka，KafkaChatServer 消费后再执行持久化和推送，适合把消息接入与处理解耦。

当前 Kafka 版本仍需要进一步解决：

- 多实例之间的在线连接归属。
- Topic/Partition 规划和同一会话有序性。
- 消费幂等、重复消息和重平衡。
- 消费失败重试与死信队列。

### 5. 单聊与群聊

单聊通过用户 ID 找在线 Client；若接收者不在线，只持久化，重新进入会话时从消息历史接口加载。

群聊读取群成员列表，只向当前进程中在线的成员推送。发送者也由后端回显，以保持前端展示结构一致。

这种方式在单实例下直观有效；多实例部署时需要 Redis/Kafka/网关维护“用户在哪台连接服务器”的映射。

### 6. Redis 缓存

缓存对象包括：

- 联系人列表。
- 我创建/加入的群。
- 群详情和成员列表。
- 用户会话和群会话列表。
- 单聊和群聊消息历史。
- 短信验证码。

当前使用 Cache Aside 思路：先读 Redis，未命中再查 MySQL并回填；修改数据后手动删除相关缓存。

风险是缓存键较多且失效逻辑分散，容易漏删。更稳妥的方向是集中缓存服务、统一 Key 生成、事务后事件通知或延迟双删。

### 7. 文件上传

文件先通过 HTTP Multipart 上传到本地 `static/files`，再通过 WebSocket 发送文件元数据和 URL。

优点是把二进制上传和实时消息通知分离；问题是：

- 原文件名可能冲突。
- 缺少扩展名、MIME 和恶意文件校验。
- 多实例不能共享本地文件。
- 下载没有访问控制。

推荐改为 UUID 文件名 + 对象存储 + 元数据表 + 临时签名 URL。

### 8. 日志和配置

- Zap 同时输出控制台和 JSON 文件日志。
- Lumberjack 负责文件大小、备份数和保存时间控制。
- 启动时自动创建日志和静态目录。
- 配置支持默认相对路径和 `KAMA_CHAT_CONFIG` 覆盖。

### 9. 后端测试现状

当前只有配置、日志和 GORM 创建用户等少量测试。建议补充：

- Service 表驱动单元测试。
- MySQL/Redis 集成测试。
- WebSocket 双客户端端到端测试。
- 群成员一致性和缓存失效测试。
- 并发、断线、重复消息和大文件测试。

## 四、前端开发总结

### 1. 页面结构

- 登录、验证码登录、注册。
- 会话列表。
- 联系人和群列表。
- 聊天详情。
- 个人资料。
- 管理员后台。

### 2. Pinia 状态管理

全局 Store 保存：

- `backendUrl`
- `wsUrl`
- `userInfo`
- `socket`

用户资料同步到 `sessionStorage`，刷新时恢复；Socket 不能序列化，因此 App 挂载时重新创建。

从 Vuex 迁移到 Pinia 后：

- `store.state.xxx` 改为 `store.xxx`。
- `commit` 改为直接调用 action。
- Router 守卫通过独立 Pinia 实例访问 Store。
- 业务组件不再依赖 Vuex API。

### 3. 路由与登录态

Router 守卫同时检查 `store.userInfo.uuid` 和 JWT，未登录只能访问登录、注册和验证码页面。

真正的安全校验在后端：Axios 统一发送 Bearer JWT，业务接口验证 JWT 并查询账号状态，管理员接口再检查数据库中的实时管理员角色。

### 4. 联系人与群聊页面

前端实现了：

- 用户和群搜索。
- 好友/入群申请。
- 新申请列表和审批操作。
- 创建群、编辑群资料、上传群头像。
- 群成员查看和移除。
- 好友删除、拉黑及取消拉黑。

联系人页和弹窗组件存在较多重复逻辑，后续可提取 composable 和 API 模块。

### 5. 聊天页面

聊天页负责：

- 获取联系人和会话信息。
- 加载用户/群消息历史。
- 监听 WebSocket 消息。
- 发送文本和文件消息。
- 展示联系人/群详情和管理弹窗。
- 处理 WebRTC Offer、Answer、ICE Candidate。

`ContactChat.vue` 代码量过大，是典型“胖组件”。建议拆成：

- `ChatHeader`
- `MessageList`
- `MessageItem`
- `ChatComposer`
- `FileUploader`
- `ContactPanel`
- `GroupSettings`
- `CallDialog`
- `useWebSocket`
- `useWebRTC`

### 6. 未完成的前端功能

- 表情包：按钮没有选择面板和发送逻辑，目前还错误调用了文件下载方法。
- 语音消息：没有录音、编码、上传和播放组件。
- 聊天记录：只有图标，没有搜索或抽屉页面。
- 全文复制：只有图标，没有 Clipboard API 逻辑。
- 文件上传：能用，但交互和异常处理较弱。

## 五、项目优化总结

### 1. 已经完成的优化

#### Windows 与 Docker 适配

- 新增 MySQL、Redis Docker Compose。
- 使用 13306、16379 避免与本机服务冲突。
- MySQL 从 Linux Unix Socket 改为 TCP。
- 配置和日志使用跨平台相对路径。
- 自动创建日志和静态目录。
- 本地开发改为 HTTP 8000 + 8080，不强制 Linux TLS 证书。

#### 开发体验

- 新增本地固定短信验证码，避免依赖阿里云。
- API 和 WebSocket 地址支持环境变量。
- 编写 Windows 运行指南。
- 准备 20 位好友、7 个群和 40 条主题消息，便于演示。

#### 前端状态管理

- Vuex 迁移到 Pinia。
- 用户信息持久化到 sessionStorage。
- 页面刷新后恢复用户信息并重建 WebSocket。

#### 认证安全

- 新注册密码使用 bcrypt，旧明文密码在首次成功登录后自动升级。
- 增加 HS256 JWT，固定验证算法并校验 issuer、audience、issued-at 和 expiration。
- HTTP API 分为公开、登录保护和管理员保护路由组。
- WebSocket 使用 `Sec-WebSocket-Protocol` 发送 JWT，从签名后的 subject 获取用户身份。
- 服务端覆盖 WebSocket 消息中的发送者 ID、昵称和头像，避免客户端伪造身份。

#### 界面

- 动漫背景替换为米白色。
- 修复长群名换行导致的聊天头部布局异常。

### 2. 建议优先完成的优化

#### P0：安全

1. 增加 Refresh Token、Access Token 吊销、密钥轮换和退出失效机制。
2. 为所有 owner_id/user_id 类请求补充对象级授权，不能只验证“已经登录”。
3. 限制 CORS 和 WebSocket Origin。
4. 加入 CSP 并系统治理 XSS，降低 sessionStorage token 被窃取风险。
5. 验证文件类型、大小和访问权限。

#### P0：稳定性

1. WebSocket 心跳、断线检测和指数退避重连。
2. 消息客户端 ID、服务端 ID、ACK、超时重试和幂等去重。
3. 连接关闭时统一释放 Client、Channel 和媒体资源。
4. 完善异常关闭、账号禁用和 Token 过期时的统一清理。

#### P1：数据一致性

1. 好友审批、入群、退群、踢人和解散群使用数据库事务。
2. 为关系表增加联合唯一索引。
3. 重新设计群成员表，减少 JSON 与关系表双写。
4. 统一 Redis Key 和缓存失效策略。

#### P1：工程化

1. 拆分 ContactChat 大组件。
2. 建立 `api/` 请求模块和统一 Axios 拦截器。
3. 建立 `useWebSocket`、`useWebRTC` composable。
4. 后端减少 Channel 与 Kafka 两套重复业务代码。
5. 增加单元、集成和 E2E 测试。
6. 容器化后端和前端，增加健康检查与 CI/CD。

#### P2：性能与分布式

1. 消息历史改为游标分页，避免一次加载全部。
2. 使用对象存储和 CDN 承载文件。
3. 在线状态放入 Redis，并设计多实例路由。
4. Kafka 按会话或接收者 Key 分区以保证局部有序。
5. 增加限流、指标监控、链路追踪和告警。

## 六、面试常见问题及回答

### 1. 这个项目解决了什么问题？

答：它是一个完整的即时通讯学习项目，覆盖用户和联系人体系、单聊群聊、在线消息推送、离线消息持久化、缓存、文件传输、音视频信令和后台管理。与普通 CRUD 项目相比，重点在长连接管理、并发消息转发和消息状态处理。

### 2. 为什么后端选择 Go？

答：IM 系统需要维护长连接和大量并发 I/O。Go 的 goroutine 和 Channel 适合表达连接读写、消息队列和后台消费流程；标准库网络能力成熟，部署也相对简单。但 Go 并不自动解决可靠性问题，心跳、重连、背压和幂等仍需要业务设计。

### 3. 为什么同时使用 HTTP 和 WebSocket？

答：登录、联系人、群管理、历史消息等请求是明确的一问一答，适合 HTTP；聊天和通话信令需要服务端主动推送，适合 WebSocket。文件二进制先走 HTTP 上传，上传成功后再通过 WebSocket 发送文件元数据，避免大文件占用实时消息通道。

### 4. 一条单聊消息如何到达对方？

答：前端通过 WebSocket 发送 JSON；Client.Read 将消息放入 Transmit Channel；Server 解析并写入 MySQL，然后根据接收用户 ID 查询在线 Client，将响应放进 SendBack；Client.Write 写回 WebSocket并更新消息状态。接收者离线时消息仍保存在数据库，之后加载历史消息。

### 5. 群聊消息如何广播？

答：后端根据群 ID 查询 `group_info.members`，反序列化成员 UUID，遍历在线 Client Map，把同一条消息投递给在线成员，同时把消息持久化。这个方案适合单实例；多实例时需要增加跨节点在线路由或 Pub/Sub/Kafka 通知。

### 6. Channel 和 Kafka 模式有什么区别？

答：Channel 模式是进程内存队列，部署简单、延迟低，适合本地和单实例；Kafka 模式让接入和处理解耦，具备持久化、削峰和水平扩展基础，但需要处理分区有序、重复消费、幂等、重平衡和在线连接归属。当前本地默认 Channel，Kafka 是可切换实现。

### 7. 系统如何处理离线消息？

答：消息先写 MySQL，再尝试实时推送。用户离线时没有对应 Client，实时推送跳过，但消息仍在数据库；用户重新打开会话时通过历史消息接口加载。当前还不是严格的已送达/已读体系，只记录基础发送状态。

### 8. Redis 在项目中做什么？

答：用于保存一分钟有效的验证码，以及缓存联系人、群详情、群成员、会话列表和消息历史。读取时先查 Redis，未命中再查 MySQL并回填；修改后手动删除缓存。优点是降低重复查询，难点是缓存失效点分散，容易出现短时不一致。

### 9. 为什么需要 Session 表，直接查 Message 不行吗？

答：会话列表需要快速展示联系人、头像、最近消息和最近时间。如果每次都从大消息表按联系人聚合，查询成本较高。Session 是面向列表页的聚合结果；消息表负责完整历史。后续还要保证新消息与 Session 最近消息的原子更新。

### 10. 如何保证消息顺序？

答：当前单进程 Channel 基本按进入同一 Channel 的顺序处理，数据库以时间字段展示，但这不是严格的分布式顺序保证。更完整的方案是为每个会话生成单调序列号，Kafka 按会话 ID 分区，客户端按序列去重和补拉缺失消息。

### 11. 当前消息可靠性做到了什么程度？

答：实现了先持久化、再向在线用户推送，并在 WebSocket 写成功后更新发送状态。但缺少客户端 ACK、超时重试、唯一客户端消息 ID、幂等表和死信处理，因此不能宣称严格不丢不重。

### 12. WebSocket 如何管理并发？

答：每个 Client 有独立 Read/Write goroutine 和发送缓冲；Server 用 Map 保存在线连接，用 Mutex 保护 Map，并通过 Login、Logout、Transmit Channel 串联事件。需要继续补充心跳、单连接写锁、异常断线清理和慢客户端治理。

### 13. 为什么从 Vuex 换成 Pinia？

答：项目使用 Vue 3，Pinia API 更简洁、类型推导和模块化体验更好，也是 Vue 官方推荐的状态管理方案。迁移后组件直接访问 Store 属性并调用 action，去除了 mutation/commit 样板代码。用户信息保存在 sessionStorage，刷新时恢复。

### 14. Windows 适配主要解决了什么？

答：原项目依赖 Linux 绝对配置路径、MySQL Unix Socket、Linux TLS 证书和 443 端口。本地化改造加入 Docker Compose，将数据库连接改为 TCP，配置改为相对路径和环境变量，开发环境改为 HTTP 8000/8080，并自动创建日志和静态目录。

### 15. 文件上传为什么不直接走 WebSocket？

答：大文件会阻塞实时消息通道并增加内存和分片处理复杂度。当前用 HTTP Multipart 上传文件，WebSocket 只传文件 URL、名称、类型和大小，更容易控制。不过当前本地磁盘方案还应升级为对象存储和签名 URL。

### 16. 音视频通话是如何实现的？

答：浏览器使用 WebRTC 建立媒体连接，WebSocket 只负责转发呼叫、接受/拒绝、SDP Offer/Answer 和 ICE Candidate 等信令。WebRTC 媒体通常点对点传输。当前代码已有信令和页面逻辑，但生产环境还需要 HTTPS、STUN/TURN、网络兼容和资源清理验证。

### 17. 当前最大的安全问题是什么？

答：第一轮安全升级已经完成 bcrypt、历史密码渐进迁移、HTTP Bearer JWT、管理员中间件和 WebSocket JWT 身份校验。当前最大的剩余问题是缺少 Refresh Token 与主动吊销、部分接口缺少对象级授权、CORS/WebSocket Origin 仍过宽，以及静态文件缺少访问控制。

### 18. 如何解决群成员双写一致性？

答：当前群成员既在 JSON 数组中，也在 user_contact 中。短期可以用事务同时更新并在提交后清缓存；更合理的长期方案是单独设计 group_member 表作为唯一事实来源，群人数可以实时统计或异步维护，避免 JSON 和关系表双写。

### 19. 如果要支持多实例，最先改什么？

答：先把认证、在线状态和连接路由设计清楚。在线连接仍在各实例内存中，但 Redis 记录 userID → gatewayID；消息通过 Kafka 或 Redis Pub/Sub 发到对应网关；文件迁移对象存储；Session 和消息消费需要幂等；负载均衡层处理 WebSocket 长连接。

### 20. 你在这个项目中做了哪些具体优化？

答：我重点完成了 Windows + Docker 本地运行适配，将 MySQL Unix Socket 改为 TCP，移除本地对 Linux TLS 证书和 443 的强依赖；增加跨平台配置、开发验证码和 Pinia；进一步完成 bcrypt 历史密码迁移、HTTP JWT、管理员中间件和 WebSocket JWT 鉴权，并让发送者身份以服务端认证结果为准。

回答时只选择自己真正完成且能解释代码细节的部分，不要把所有原项目功能都描述成个人独立开发。

### 21. 项目遇到过哪些问题，如何排查？

答：本地运行时主要遇到跨平台路径、证书、端口和 Docker 服务连接问题。我先从启动日志和配置入手，确认后端依赖 MySQL/Redis，再用 Docker Compose 固定服务和端口；把硬编码绝对路径替换为相对路径和环境变量；Windows 下 npm.ps1 被策略拦截时改用 npm.cmd；数据库直改后清 Redis，避免旧缓存影响验证。

### 22. 下一步最值得做的功能是什么？

答：密码哈希、JWT 和 WebSocket 鉴权已经完成。下一步优先补 Token 吊销/刷新、对象级授权、CORS/Origin 收紧，以及心跳重连、客户端消息 ID、ACK 和幂等；之后再补表情包、语音、聊天记录搜索和对象存储。

## 七、面试表达注意事项

- 不要说“系统保证消息绝对不丢”，当前没有完整 ACK 和重试闭环。
- 不要说“已经实现分布式部署”，当前有 Kafka 路径，但本地默认单实例，在线连接仍在进程内。
- 不要说“JWT 加密密码”：密码使用 bcrypt 单向哈希，JWT 用于签名身份 Claims，两者职责不同。
- 可以说“设计并实现了 Channel/Kafka 可切换的消息处理路径”，但要说明当前默认 Channel。
- 可以说“实现基础文件传输”，同时主动指出本地磁盘和文件名冲突问题。
- 音视频可描述为“基于 WebRTC、WebSocket 传递信令”，不要夸大为成熟会议系统。
- 回答个人贡献时可重点讲 Windows/Docker、Pinia、开发验证码、bcrypt 渐进迁移、JWT/WebSocket 鉴权、演示数据和界面修复。

## 八、适合继续开发的任务清单

1. 增加 Refresh Token、Token 吊销和密钥轮换。
2. 补齐对象级授权并收紧 CORS/WebSocket Origin。
3. 继续完善统一 WebSocket 服务，加入心跳和重连。
4. 实现客户端消息 ID、ACK、重试和幂等。
5. 实现表情面板和 Unicode Emoji 消息。
6. 实现聊天记录分页、搜索和全文复制。
7. 实现语音录制、上传和播放。
8. 文件改为 UUID 命名并迁移 MinIO/OSS。
9. 拆分 `ContactChat.vue` 和重复 API 逻辑。
10. 增加后端集成测试与前端 E2E 测试。
11. 统一 Redis 缓存失效策略：取消注释中遗留的缓存更新代码，建立集中的缓存 Key 管理和失效机制，对写操作采用"先更新 MySQL → 再主动删除/刷新 Redis"的固定模式。
12. 好友审批、入群、退群、踢人和解散群操作使用数据库事务，保证多步操作原子性。
13. 关系表增加联合唯一索引，防止重复的好友/群成员关系记录。
