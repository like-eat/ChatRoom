# KamaChat 项目快速了解

> 用途：供后续开发者或 AI 在重新打开项目时快速建立上下文。优先阅读本文件，不必一开始遍历全部代码。
>
> 内容基于当前工作区代码整理，最后核对日期：2026-08-13。已同步精简重构后的代码状态。

## 1. 一句话介绍

KamaChat 是一个前后端分离的仿微信即时通讯系统：后端使用 Go + Gin + GORM，通过 WebSocket 实现实时单聊和群聊文本消息，通过 MySQL 持久化业务数据、Redis 缓存热点数据，并保留 Channel 与 Kafka 两种消息转发模式；前端使用 Vue 3 + Pinia + Element Plus。

当前工作区已经在原项目基础上完成 Windows + Docker 本地运行适配、Vuex→Pinia 迁移和 JWT 安全加固，并精简删除了短信验证码、音视频通话、文件/语音消息、表情包、拉黑等冗余功能，聚焦核心文本聊天闭环。

## 2. 当前运行架构

```text
浏览器（Vue 3，127.0.0.1:8080）
    ├── HTTP/Axios ────────> Gin API（127.0.0.1:8000）
    └── WebSocket ─────────> /wss（Sec-WebSocket-Protocol 携带 JWT）
                                  │
                                  ├── Channel 聊天服务（当前默认）
                                  │      ├── 在线客户端 Map
                                  │      ├── Login/Logout Channel
                                  │      └── Transmit Channel
                                  ├── Kafka 聊天服务（可选）
                                  ├── GORM ──> MySQL 8（Docker，主机端口 13306）
                                  ├── go-redis ──> Redis（Docker，主机端口 16379）
                                  └── 本地磁盘 ──> static/avatars（头像）
```

注意：当前 `compose.yaml` 只容器化了 MySQL 和 Redis，Go 后端及 Vue 前端仍在 Windows 主机的 VS Code 终端运行。

## 3. 技术栈

### 后端

- Go 1.20
- Gin 1.10：HTTP API、静态资源和 WebSocket 升级入口
- GORM + MySQL 8：业务数据持久化和自动建表
- Gorilla WebSocket：长连接、实时消息转发
- go-redis：联系人、群、会话和消息历史缓存
- Go Channel：本地默认消息队列
- kafka-go：可选 Kafka 消息转发模式
- Zap + Lumberjack：结构化日志和日志切割

### 前端

- Vue 3（Options API + Composition API 混用）
- Vue CLI 5，项目不是 Vite
- Vue Router 4
- Pinia 2（已替换 Vuex）
- Element Plus
- Axios
- 原生 WebSocket

### 本地开发

- Windows 10/11
- Docker Desktop（Linux 容器模式）
- Node.js + npm
- VS Code / PowerShell

## 4. 目录地图

```text
api/v1/                         Gin 控制器，参数绑定和统一响应
cmd/kama_chat_server/main.go    后端程序入口
configs/config.toml             当前默认本地配置
docs/Windows运行指南.md          Windows 启动说明
docs/mock-data-expanded.sql     扩展 Mock 数据脚本
internal/config/                TOML 配置加载
internal/dao/                   GORM/MySQL 初始化和 AutoMigrate
internal/dto/request/           API/WebSocket 请求结构
internal/dto/respond/           API/WebSocket 响应结构
internal/https_server/          Gin、CORS、静态目录和路由注册
internal/model/                 六张核心表的 GORM 模型
internal/service/chat/          Channel/Kafka 聊天服务器与 WebSocket Client
internal/service/gorm/          用户、好友、群、会话、消息业务
internal/service/kafka/         Kafka Reader/Writer 初始化
internal/service/redis/         Redis 基础操作
pkg/                            常量、枚举、日志、随机 ID、TLS 工具
static/avatars/                 本地头像资源
test/                           少量 Go 基础测试
web/chat-server/                Vue 前端工程
compose.yaml                    MySQL、Redis 本地容器
```

## 5. 核心数据模型

后端启动时通过 GORM `AutoMigrate` 自动创建以下表：

| 表 | 作用 | 关键字段 |
|---|---|---|
| `user_info` | 用户账号和资料 | UUID、手机号、bcrypt 密码哈希、头像、管理员、状态 |
| `group_info` | 群资料 | UUID、群主、成员 JSON、人数、状态 |
| `user_contact` | 用户与好友/群的关系 | user_id、contact_id、类型、关系状态 |
| `contact_apply` | 好友或入群申请 | 申请人、目标、类型、申请状态、附言 |
| `session` | 会话列表 | 发起人、接收对象、最近消息、最近时间 |
| `message` | 消息历史 | 会话、类型、发送/接收者、内容、URL、状态 |

UUID 首字母具有业务含义：

- `U`：用户
- `G`：群聊
- `S`：会话
- `M`：消息
- `A`：联系人申请

代码多处通过接收 ID 的首字母判断单聊和群聊，不要随意改变此约定。

消息类型约定：当前仅文本消息（`type=0`）。语音、文件、通话等类型已随精简重构一并删除。

## 6. 主要业务流程

### 注册与登录

1. 注册调用 `/register`（手机号 + 昵称 + 密码），密码以 bcrypt 哈希入库。
2. 密码登录调用 `/login`（手机号 + 密码）。
3. 登录成功后服务端签发 HS256 JWT，前端把用户信息和 token 写入 Pinia 与 `sessionStorage`。
4. Axios 自动添加 `Authorization: Bearer <JWT>`。
5. 前端连接 `ws://127.0.0.1:8000/wss`，通过 `Sec-WebSocket-Protocol` 发送 JWT；服务端从 JWT 的 `sub` 获取用户 UUID，不信任浏览器传入的用户 ID。
6. 旧数据库中的明文密码仅用于兼容迁移：首次正确登录后会自动替换为 bcrypt 哈希；新注册账号直接保存 bcrypt。

### 单聊消息

1. 前端先通过 `/session/openSession` 获取或创建会话。
2. 文本消息序列化后通过 WebSocket 发送。
3. WebSocket Client 的 Read 协程把消息送入 Channel 或 Kafka。
4. 聊天服务器把消息写入 MySQL，初始状态为未发送。
5. 接收者在线时推送给接收者；同时推送给发送者完成回显。
6. Write 协程成功写入 WebSocket 后把消息状态更新为已发送。
7. 接收者离线时消息仍保存在 MySQL，之后通过历史接口加载。

### 群聊消息

1. 消息 `receive_id` 以 `G` 开头。
2. 后端读取 `group_info.members` JSON 成员数组。
3. 遍历成员并向当前在线客户端广播。
4. 消息持久化到 MySQL，并同步更新已有 Redis 消息缓存。

### 联系人和群

- 好友：申请、审批通过、拒绝、删除。
- 群聊：创建、申请加群、审批、更新群资料、成员移除、退群、解散。
- 关系状态保存在 `user_contact`；申请过程保存在 `contact_apply`。
- 群成员同时存在于 `group_info.members` JSON 和 `user_contact`，修改时必须维护一致性。

> 文件消息、语音消息、表情包、音视频通话、拉黑等已在精简重构中删除，仅保留文本消息。

## 7. 功能完成度

| 模块 | 当前状态 |
|---|---|
| 密码注册/登录 | 已实现 |
| bcrypt 密码保护与旧密码迁移 | 已实现 |
| HTTP JWT 与管理员鉴权 | 已实现基础版本 |
| WebSocket JWT 身份校验 | 已实现；发送者身份由服务端覆盖 |
| 个人资料与头像上传 | 已实现基础版本 |
| 好友申请、审批、拒绝、删除 | 已实现 |
| 创建群、申请加群、群资料、成员管理、退群/解散 | 已实现 |
| 会话列表、单聊、群聊、历史消息 | 已实现基础版本 |
| 文本消息 | 已实现 |
| Channel/Kafka 双模式消息转发 | 已实现；默认 Channel |
| Redis 缓存（联系人/群/会话/消息历史） | 已实现 Cache Aside |
| 管理员后台 | 已实现用户/群启禁用、删除和管理员设置 |
| ~~短信验证码、音视频通话、文件/语音消息、表情包、拉黑~~ | 已删除（精简重构） |

## 8. Windows 完整启动方式

### 环境要求

- Docker Desktop 已启动
- Go 1.20+
- Node.js 与 npm

### 终端一：基础设施

在项目根目录：

```powershell
docker compose up -d
docker compose ps
```

### 终端二：后端

在项目根目录：

```powershell
go mod download
go run .\cmd\kama_chat_server
```

后端地址：`http://127.0.0.1:8000`

WebSocket：`ws://127.0.0.1:8000/wss`

### 终端三：前端

```powershell
Set-Location .\web\chat-server
npm.cmd install
npm.cmd run serve
```

浏览器访问：`http://127.0.0.1:8080`

PowerShell 可能禁止执行 `npm.ps1`，因此 Windows 下优先使用 `npm.cmd`。

### 停止

前后端终端按 `Ctrl+C`，然后：

```powershell
docker compose down
```

不要随意执行 `docker compose down -v`，该命令会删除本项目 MySQL 和 Redis 命名卷。

## 9. 关键配置

配置文件：`configs/config.toml`

```toml
[mainConfig]
host = "127.0.0.1"
port = 8000

[mysqlConfig]
host = "127.0.0.1"
port = 13306
databaseName = "kama_chat"

[redisConfig]
host = "127.0.0.1"
port = 16379

[jwtConfig]
issuer = "kama-chat"
audience = "kama-chat-web"
expireHours = 24

[kafkaConfig]
messageMode = "channel"
```

可用 `KAMA_CHAT_CONFIG` 指定其他配置文件：

```powershell
$env:KAMA_CHAT_CONFIG = "C:\path\to\config.toml"
go run .\cmd\kama_chat_server
```

前端地址可覆盖：

```powershell
$env:VUE_APP_BACKEND_URL = "http://127.0.0.1:8000"
$env:VUE_APP_WS_URL = "ws://127.0.0.1:8000"
npm.cmd run serve
```

## 10. 当前本地演示数据

- 用户：`bufan`，登录手机号：`15863343113`，密码：`123456`（已重置为已知值，供演示使用）
- 双账号演示：好友 `小林`，登录手机号：`13910000001`，密码：`123456`（已重置，与 bufan 有现成会话，可直接互发消息）
- bufan 已设置为管理员
- 20 位 Mock 好友
- 7 个 Mock 群聊
- 40 条 Mock 消息，主题包含天气、饮食和运动
- 扩展脚本：`docs/mock-data-expanded.sql`

这些运行数据主要存在 Docker MySQL 命名卷中，不等同于 Git 代码。扩展脚本是在已有基础 Mock 数据之上补齐数量，重新建立全新数据库时需要先确认基础数据是否存在。

## 11. 当前工作区相对原项目的主要改造

- 增加 Windows + Docker 运行方案。
- Linux Unix Socket 数据库连接改为 TCP。
- 去除本地开发对 Linux TLS 证书和 443 端口的强依赖。
- 配置路径由硬编码 Linux 绝对路径改为相对路径，并支持环境变量。
- 自动创建日志和头像目录。
- 精简重构：删除短信验证码、音视频通话、文件/语音消息、表情包、拉黑、加群方式等冗余功能，聚焦文本聊天核心闭环。
- Vuex 迁移为 Pinia，并更新所有页面和组件调用。
- 后端地址和 WebSocket 地址支持环境变量。
- 用户信息使用 `sessionStorage` 恢复，刷新后重建 WebSocket。
- 聊天主背景改为米白色；长群名保持单行显示。
- 增加演示好友、群聊、消息和本地 SVG 头像。
- 密码改为 bcrypt，并为历史明文账号提供登录后自动升级。
- 增加 HTTP Bearer JWT、管理员中间件和 WebSocket JWT 身份校验。
- WebSocket 发送者 ID、昵称和头像以服务端认证结果为准，防止客户端伪造。

## 12. 已知问题和高优先级风险

### 安全

- 本地配置中的 JWT 密钥只适合开发；生产必须通过 `KAMA_CHAT_JWT_SECRET` 提供高熵随机密钥。
- 当前只有 24 小时 Access Token，没有 Refresh Token、主动吊销列表和密钥轮换机制。
- token 存放在 `sessionStorage`，仍需通过 CSP、依赖审计和输入输出转义降低 XSS 风险。
- 部分业务仍需继续补充“当前用户只能操作自己资源”的对象级授权校验。
- WebSocket Origin 仍允许任意来源。
- CORS 当前允许所有来源。
- 静态文件下载目前没有访问权限校验。

### 可靠性

- 缺少 WebSocket 心跳、自动重连、ACK、超时重试和消息幂等。
- 同一用户重复连接会覆盖在线 Map 中的旧连接。
- 服务停止时直接关闭 Channel，协程退出和连接清理还需加固。
- Channel 模式是单进程内存状态，不能直接支持多实例水平扩展。

### 数据一致性

- 群成员存在 JSON 和联系人关系两份数据。
- 多步骤好友/群操作需要系统性补充数据库事务。
- Redis 主要使用手动失效，容易出现漏删缓存。

### 前端

- WebSocket 事件处理在登录页、注册页和 App 中重复。
- `ContactChat.vue` 文件很大，职责过多，应拆分消息列表、输入区、群设置等模块。

### 部署

- Docker Compose 尚未容器化 Go 后端和 Vue 前端。

## 13. 下次接手建议阅读顺序

如果只是恢复上下文，按以下顺序阅读即可：

1. 本文件。
2. `docs/Windows运行指南.md` 和 `configs/config.toml`。
3. `cmd/kama_chat_server/main.go`、`internal/https_server/https_server.go`。
4. `internal/service/chat/client.go`、`internal/service/chat/server.go`。
5. `internal/model/*.go` 和目标业务对应的 `internal/service/gorm/*.go`。
6. `web/chat-server/src/store/index.js`、`router/index.js`。
7. 根据任务选择具体 Vue 页面，不要默认通读巨大的 `ContactChat.vue`。

## 14. 常用验证命令

```powershell
# 查看容器
docker compose ps

# 后端测试
go test ./...

# 前端构建
Set-Location .\web\chat-server
npm.cmd run build

# 搜索接口注册
rg -n "GE\.(GET|POST)" internal/https_server/https_server.go

# 搜索前端接口使用
rg -n "axios\.(get|post)" web/chat-server/src
```

前端生产构建目前可以成功，但会出现 vendor 包体积超过 Vue CLI 推荐阈值、Browserslist 数据较旧等警告。
